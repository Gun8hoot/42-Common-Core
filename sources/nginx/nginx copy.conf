events {
	worker_connections 1024;
}

http {
	server {
		listen 443 ssl;

		server_name https_reverse_proxy;

		ssl_certificate /run/secrets/nginx.crt;
		ssl_certificate_key /run/secrets/nginx.key;

		location / {
			root /var/www/test.html
		}
		return 200;
	}
	server {
		listen 80;

		server_name http_redirect;
		location / {
			root /var/www/test.html
		}
		return 200 ;
	}
}
